# การติดตั้ง Rust

## ติดตั้งด้วย rustup

**rustup** เป็นเครื่องมืออย่างเป็นทางการสำหรับติดตั้งและจัดการ Rust

### สำหรับ Windows

1. ดาวน์โหลด rustup จาก [https://rustup.rs](https://rustup.rs)
2. เปิดไฟล์ที่ดาวน์โหลดมาและทำตามขั้นตอน
3. อาจต้องติดตั้ง [Visual Studio Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/) ด้วย

### สำหรับ macOS และ Linux

เปิด Terminal แล้วพิมพ์คำสั่ง:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

ทำตามขั้นตอนที่แสดงบนหน้าจอ

---

## ตรวจสอบการติดตั้ง

หลังติดตั้งแล้ว ปิดและเปิด Terminal ใหม่ แล้วพิมพ์:

```bash
rustc --version
```

ถ้าติดตั้งสำเร็จ จะเห็นข้อความคล้ายๆ นี้:

```text
rustc 1.83.0 (90b35a623 2024-11-26)
```

ลองตรวจสอบ Cargo ด้วย:

```bash
cargo --version
```

จะได้:

```text
cargo 1.83.0 (5ffbef321 2024-10-29)
```

---

## อัปเดต Rust

Rust ออกเวอร์ชันใหม่ทุก 6 สัปดาห์ อัปเดตได้ด้วย:

```bash
rustup update
```

---

## เลือก IDE / Text Editor

แนะนำให้ใช้ **VS Code** พร้อม extension:

1. [rust-analyzer](https://marketplace.visualstudio.com/items?itemName=rust-lang.rust-analyzer) - สำคัญมาก!
2. [Even Better TOML](https://marketplace.visualstudio.com/items?itemName=tamasfe.even-better-toml)
3. [Error Lens](https://marketplace.visualstudio.com/items?itemName=usernamehw.errorlens)

หรือจะใช้ **RustRover** ซึ่งเป็น IDE เฉพาะสำหรับ Rust จาก JetBrains ก็ได้

---

## สรุป

✅ ติดตั้ง Rust ด้วย rustup  
✅ ตรวจสอบด้วย `rustc --version`  
✅ ติดตั้ง VS Code + rust-analyzer

👉 ต่อไป: [Hello World](./02-hello-world.md)
